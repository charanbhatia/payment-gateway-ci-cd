apiVersion: v1
kind: ConfigMap
metadata:
  name: payment-gateway-config
  namespace: default
data:
  APPLICATION_NAME: "payment-gateway"
  SERVER_PORT: "8080"
  LOG_LEVEL: "INFO"
  # Database configuration (use Kubernetes Secrets for credentials)
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "paymentdb"
